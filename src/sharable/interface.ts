export interface ITemplate {
  name: string;
  description: string;
  link: string;
  created: string;
  category: string[];
}
export interface ISortState {
  order: string;
  date: string;
  category: string;
}
